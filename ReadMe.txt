백준 5612 문제 풀이 중입니다.


문제 풀이를 완료했습니다.

들어오는 자동차의 수와 나가는 자동차의 수, 현재 터널에 존재하는 자동차의 수를 통해 앞으로 터널에 존재하게 될 자동차으 개수를

파악하고 파악된 자동차의 개수가 현재까지 매 분마다 계산된 자동차의 개수보다 클 경우 현재 계산된 자동차의 개수를 최대값으로 변경시켜주고

만일 현재 계산된 자동차의 개수가 0보다 작을 경우 바로 0을  출력, 그렇지 않고 정해진 분이 지났을 경우 그때까지 계산된 최대 값을 출력하도록

구현했습니다.
