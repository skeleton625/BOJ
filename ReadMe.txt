백준 10990 문제 풀이 중입니다.

문제 풀이를 완료했습니다.

이 코드는 개선이 가능합니다.

해당 코드에서 첫 번째 반복문의 ++ㅑ && printf("\n")의 경우에서 i++로 바꿀 경우 첫 번째 반복에서 i가 0이기 때문에 printf("\n")를 실행하지 않고

다음 반복으로 넘어갑니다.

두 번째 반복문의 경우 띄어쓰기와 별의 개수를 보면 첫 번째 줄은 입력한 t, 그 다음 줄은 t+1, 그 다음 줄은 t+2 이므로 조건은 j <= t + i가 됩니다.

또한 출력을 할 때 중간의 공간이 계속 늘어나게 출력하면 되므로 조건을 (j == t - i) || (j == t + i)으로 하면 됩니다.
