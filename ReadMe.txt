백준 13410 문제 풀이 중입니다.

문제 풀이를 완료했습니다.

해당 코드는 단의 수 N을 기준으로 항의 수 K만큼 곱함과 동시에 곱한 수를 뒤집어 최대값과 비교하면서

뒤집은 수가 최대값보다 클 경우 최대값과 바꿔주는 방법으로 구현했습니다.

여기서 숫자 n을 뒤집는 함수 reverse를 따로 구현해 사용했습니다.

reverse 함수의 경우 n이 0이 될 때까지 나눠주면서 n의 10으로 나눈 나머지 값과 0으로 초기화된 res

에 10을 곱해 다시 res에 입력하는 방식을 통해서 n을 뒤집어 res에 저장해 반환했습니다.
