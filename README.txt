백준 알고리즘 1992 문제 풀이 중입니다.

해당 문제 풀이를 완료했습니다.

해당 문제에서는 분할 정복 알고리즘을 사용해 문제를 풀이했습니다.

해당 Matrix를 위_오른족, 위_왼쪽, 아래_오른쪽, 아래_왼쪽 총 4개로 분할하는 방법을 Matrix의 가장 작은 4개의

위, 아래_왼쪽, 오른쪽 값이 모인 상태까지 반복합니다.

거기서부터 위, 아래_왼쪽, 오른쪽 값들을 비교하면서 같을 경우 하나의 값으로 변경해 반환하고 그렇지 않을 경우

전체 값을 가로'()'로 묶어 반환해줍니다.

단, 현 문제에서 주의해야할 점은 가장 작은 4개의 Matrix가 아닌 그 이후 단계에서 4개의 위,아래_왼쪽 오른쪽

문자열이 같을 경우가 존재할 수도 있는데 이 상태는 가로'()'로 묶인 값이 존재할 수가 있습니다.

그렇기 때문에 해당 값들은 하나의 값으로 통일시켜주는 것이 아니라 4개의 값 모두 살린 상태로 반환해줘야 합니다.

