백준 알고리즘 11725 문제 풀이 중입니다.

문제 풀이를 완료했습니다.

해당 코드는 BFS 알고리즘을 사용해 문제를 풀이했습니다.

입력값을 토대로 그래프(트리)를 생성하는데 두 입력 노드 중 어느 노드가 부모노드인지 확인할 수 없으므로 양뱡항으로 연결해 줍니다.

그 다음, 루트 노드인 1부터 BFS 알고리즘을 실행합니다.

루트 노드부터 진행하면서 연결된 다음 노드를 발견할 시, 다음 노드의 부모 노드를 현재 노드로 정의해 줍니다.

여기서 그래프는 양방향 그래프 이므로 자식 노드에서 부모노드로 올라갈 수 도 있기 때문에, 방문했던 노드는 다시 방문하지 않도록 표시를 해줘야 합니다.

표시를 해주고 다음 노드를 방문할 때, 현재 노드가 방문한 노드인지 확인하고 방문하지 않았을 경우 위의 경우를 따르고 방문 했으면

해당 노드는 무시하고 큐에서 다음 노드를 확인해 위의 경우를 반복해 줍니다.

이를 반복해 모든 노드를 순회하게 되면, 루트 노드인 1을 제외한 나머지 노드의 부모 노드를 파악할 수 있게 됩니다.
